/*
CREATE TABLE IF NOT EXISTS transactions (
    hash        text,
    nonce       numeric,
    block_hash  text,
    block_num   numeric,
    tx_index    numeric,
    from_addr   text,
    to_addr     text,
    value       text,
    gas         numeric,
    gas_price   numeric,
    input       text
)
*/